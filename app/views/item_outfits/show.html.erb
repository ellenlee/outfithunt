<section class="request-photo flex-center">
  <%= image_tag @outfit.image.file_path %>
</section>

<section class="compare-items flex-around">
  <div class="request-item">
    <%= link_to image_tag(@related_item.images.first.file_path) , item_path(@related_item) %>
  </div>
  <div class="collection">
    收藏穿搭
  </div>
</section>
<section class="related-objects full-width flex-center">
  <div class="footer flex-top">
    <% @other_items.each do |item| %>
      <div class="objects">
        <%= link_to image_tag(item.images.first.file_path), item_path(item) %>
      </div>
    <% end %>
  </div>
</section>

<script>
  $(".request-photo img").click(function(){
    console.log("i am clicked!");
    $(".request-photo").toggleClass('stretch');
  });
</script>